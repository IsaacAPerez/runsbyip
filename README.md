# RunsByIP

Monorepo containing the RunsByIP iOS app and marketing website.

## Structure

- `ios/` — RunsByIP iOS app (Xcode project)
- `web/` — RunsByIP marketing / landing site

## Development

Each subproject has its own tooling. Open `ios/` in Xcode for the app, and serve `web/` with any static file server for the site.
